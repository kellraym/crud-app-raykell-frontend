### create database named crud-app-raykell

### had to use migrate:rollback/latest in start

### image tag in docker-compose names items to be pushed to dockerhub

### how do I start the app from just an image?